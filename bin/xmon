#!/usr/bin/env bash 

scaling=0.75x0.675

if (( $# < 1 )); then
    echo "Usage : $(basename $0) [up][down]" >&2
fi

case $1 in
    up)
	xrandr --output eDP1 --auto --scale ${scaling} --output HDMI2 --auto --right-of eDP1
	~/.fehbg
	;;
    down)
	xrandr --output HDMI2 --off
	;;
esac

exit 0
