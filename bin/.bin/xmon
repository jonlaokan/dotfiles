#! /bin/bash 

if (( $# < 1 )); then
    echo "Usage : $(basename $0) [up][down]" >&2
fi

case $1 in
    up)
	xrandr --output eDP-1 --auto --output HDMI-2 --auto --scale 2x2 --right-of eDP-1
	;;
    down)
	xrandr --output HDMI-2 --off
	;;
esac

exit 0
